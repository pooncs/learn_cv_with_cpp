#include "pixel_access.hpp"

void process_at(cv::Mat& img) {
    // TODO: Iterate using img.at<uchar>(y, x) and increment each pixel
}

void process_ptr(cv::Mat& img) {
    // TODO: Iterate using img.ptr<uchar>(y) and increment each pixel
}

void process_iter(cv::Mat& img) {
    // TODO: Iterate using cv::MatIterator_<uchar> and increment each pixel
}

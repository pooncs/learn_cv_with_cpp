#include "morphology.hpp"

cv::Mat my_dilate(const cv::Mat& src) {
    // TODO: Implement Dilation
    // If any neighbor is 255, set to 255
    return cv::Mat();
}

cv::Mat my_erode(const cv::Mat& src) {
    // TODO: Implement Erosion
    // If all neighbors are 255, set to 255, else 0
    return cv::Mat();
}

#include "mat_info.hpp"
#include <sstream>

std::string get_mat_info(const cv::Mat& m) {
    // TODO: Return string with rows, cols, channels, depth, elemSize, step
    return "";
}

bool share_data(const cv::Mat& m1, const cv::Mat& m2) {
    // TODO: Check if m1.data equals m2.data
    return false;
}

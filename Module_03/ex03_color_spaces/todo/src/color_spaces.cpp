#include "color_spaces.hpp"

cv::Mat to_gray(const cv::Mat& src) {
    // TODO: Implement BGR to Gray conversion
    // Y = 0.299*R + 0.587*G + 0.114*B
    return cv::Mat();
}

cv::Mat to_hsv(const cv::Mat& src) {
    // TODO: Implement BGR to HSV conversion
    return cv::Mat();
}

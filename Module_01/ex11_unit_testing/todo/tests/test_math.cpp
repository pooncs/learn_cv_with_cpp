#include <gtest/gtest.h>
#include "MathUtils.hpp"

TEST(MathUtilsTest, Factorial) {
    // TODO: Add tests for factorial
    EXPECT_TRUE(true);
}

TEST(MathUtilsTest, Fibonacci) {
    // TODO: Add tests for fibonacci
    EXPECT_TRUE(true);
}

TEST(MathUtilsTest, Lerp) {
    // TODO: Add tests for lerp
    EXPECT_TRUE(true);
}

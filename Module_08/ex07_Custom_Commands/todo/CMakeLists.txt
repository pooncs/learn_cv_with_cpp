cmake_minimum_required(VERSION 3.15)
project(ex07_custom_commands)

set(CMAKE_CXX_STANDARD 17)

# TODO: Define the path for the generated file
# set(VERSION_HEADER "${CMAKE_BINARY_DIR}/generated/version.h")

# TODO: Add custom command to generate version.h
# add_custom_command(
#     OUTPUT ${VERSION_HEADER}
#     COMMAND ... (echo or python script)
#     COMMENT "Generating version header"
# )

# TODO: Add executable
# add_executable(app src/main.cpp ${VERSION_HEADER})

# TODO: Include directory for generated files
# target_include_directories(app PRIVATE "${CMAKE_BINARY_DIR}/generated")

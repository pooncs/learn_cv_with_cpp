#include <QApplication>
#include <QWidget>
#include <QVBoxLayout>
#include <QSlider>
#include <QLabel>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);

    // TODO: Create main widget and layout

    // TODO: Create slider and label

    // TODO: Add widgets to layout

    // TODO: Connect slider valueChanged to label setText (use QObject::connect)

    // widget.show();
    return app.exec();
}
